import React from 'react'
import Layout from 'components/Layout.js'
import { Heading, Text, Dropdown } from 'unstyled'

const NavbarPage = () => (
  <Layout>
    <Heading>NavBar</Heading>
    <Text>`NavBar` is a major navigation bar.</Text>
  </Layout>
)

export default NavbarPage
